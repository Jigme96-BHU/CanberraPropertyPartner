/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { domains: ['images.unsplash.com', 'www.canberrapropertypartners.com.au'] },
}
module.exports = nextConfig
